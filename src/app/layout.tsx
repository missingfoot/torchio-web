import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Torchio - Cut, Clip, and Convert Videos",
  description: "The fast, offline video converter for Windows, macOS & Linux. Cut, clip, and convert videos without the hassle. No account. No uploads. No cloud. Just your videos, on your machine.",
  keywords: ["video converter", "video editor", "GIF maker", "video trimmer", "offline video editor", "Windows", "macOS", "Linux"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
